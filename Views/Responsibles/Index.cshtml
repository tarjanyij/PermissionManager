@model IEnumerable<PermissionManager.Models.Responsible>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ResponsibleName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResponsibleEmail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResponsibleName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResponsibleEmail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ResponsibleID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ResponsibleID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ResponsibleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
