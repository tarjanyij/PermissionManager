@model IEnumerable<PermissionManager.Models.Rights>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RightsName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RightsValue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OfficeRights)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RightsName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RightsValue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OfficeRights.OfficeRightsID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RightsID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RightsID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RightsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
